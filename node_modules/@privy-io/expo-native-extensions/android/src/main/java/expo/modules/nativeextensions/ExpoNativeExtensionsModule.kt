package expo.modules.nativeextensions

import expo.modules.kotlin.modules.Module
import expo.modules.kotlin.modules.ModuleDefinition

class ExpoNativeExtensionsModule : Module() {
  override fun definition() = ModuleDefinition {
    Name("ExpoNativeExtensions")

    Function("vibeCheck") {
      "Hello from expo native extensions! 👋"
    }
    
    // TODO: stub out other methods and throw error on Android
  }
}
